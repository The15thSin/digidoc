[project]
name = "digidoc-backend"
version = "0.1.0"
description = "This is the backend for the DigiDoc project."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyrefly>=1.1.1",
]
